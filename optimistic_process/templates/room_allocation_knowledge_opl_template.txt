{{ model }}

execute PRINT_SOLUTION {
  var dfile = new IloOplOutputFile("{{ output_file_path }}");
  dfile.writeln("{{ column_names }}")
   for (var empl in set_of_employee_numbers) {
     for (var fl in set_of_building_floors) {
		if (0 == floor_assignments[empl][fl]){
			  continue
		}
		dfile.writeln("\"" +empl + "\",\"" +fl+ "\"")
     }
  }
  dfile.close();
  writeln("Done Printing Output")
}
